# Heartbeat

- Repo: `cat-loss-war-room`
- Current milestone: Keep the notebook demo stable while issue `#12` evidence/provenance follow-up work continues after the issue `#107` / PR `#108` deterministic evidence dedupe helper slice.
- Current status: V0 demo is stable; active runtime is still `src/war_room/` plus `notebooks/01_case_war_room.ipynb`, not a full web app. The issue `#10` stack now has canonical run/stage vocabulary in `src/war_room/orchestration.py`, typed API boundary contracts in `src/war_room/orchestration_api_contracts.py`, an in-process offline service in `src/war_room/orchestration_service.py`, an operator-facing status presentation layer in `src/war_room/orchestration_status_view.py`, a dependency-free thin transport/request-handler wrapper in `src/war_room/orchestration_transport.py`, and a dev-only standard-library HTTP adapter in `src/war_room/orchestration_http.py`. Issue `#11` has documentation-only guided-intake and run-status UX/spec slices plus deterministic Milton previews. Issue `#27` has the human release-evidence reviewer guide, top-level `reviewer_summary`, and the issue `#92` / PR `#93` `ci_reporting_summary` inventory. Issue `#12` now has named evidence adapters landed for issue `#94` / PR `#95` caselaw output, issue `#96` / PR `#97` carrier output, issue `#98` / PR `#99` weather output, and issue `#103` / PR `#105` citation-verification output; issue `#107` / PR `#108` added local deterministic `dedupe_evidence_items(...)` over canonical `EvidenceItem` rows as a helper-only utility. The helper is not integrated into audit snapshot assembly, and no `old_id -> retained_id` remapping exists yet; the full V2 evidence graph, dedupe integration, persistence layer, review workflow, provenance-through-edits, dashboard, UI, and production API remain future work.
- Current branch: `docs/final-post-dedupe-helper-sync`
- Last validated: 2026-05-20 for this docs sync via `git diff --check`, `python -m pytest -q` (`462 passed in 34.96s`), and `python -m war_room --verify` (embedded `462 passed in 30.77s`); verify manifest `runs/verify/2026-05-20_docs-final-post-dedupe-helper-sync_20260520t151849z.json`.
- Current focus: Keep current-state docs aligned after PR `#108` while preserving issue `#12` as an umbrella for remaining dedupe integration, provenance, persistence, API/UI, review-workflow, and full evidence-graph work.
- Hot files: `README.md`, `CLAUDE.md`, `docs/heartbeat.md`, `docs/HANDOFF.md`, `docs/repo-brief.md`, `docs/ROADMAP.md`, `docs/V2_EVIDENCE_SCHEMA.md`, `docs/V2_RELEASE_RUBRIC.md`, `docs/V2_OPEN_ISSUE_AUDIT.md`, `docs/V2_BLUEPRINT.md`, `docs/ISSUE_12_REMAINING_SCOPE.md`, `docs/ISSUE_27_RELEASE_EVIDENCE_REVIEW_GUIDE.md`, `docs/V2_ISSUE_MAP.md`, `docs/SESSION_LOG.md`
- Blockers: None for the docs-only post-PR108 current-state sync.
- Do not touch this sprint: API framework, background queue, database, auth, dashboard, UI design, repo rename, broad product rewrites, placeholder V2 directories as live runtime, dependency churn without approval.
- Related repos: None documented in-repo; treat this repo as the working source of truth.
- Latest session log: `docs/SESSION_LOG.md` session 137
- Next best task: Pause implementation and review the remaining `#12` roadmap. The likely next child should be one of: a provenance-safe dedupe integration plan with `old_id -> retained_id` mapping, deterministic dedupe integration into audit snapshot assembly, provenance link hardening across memo claims / evidence clusters / review events, or citation-quality fixture regression under `#13` / `#14`. Do not treat the landed adapters or helper-only dedupe utility as completion of the full evidence/provenance graph or as Beta-ready, Pilot-ready, production-ready, or a shipped web app.
- Owner: Not explicitly named in-repo; maintained for the Merlin Law Group demo effort.
